# TdrCreator – Docker-Umgebungsvariablen
# Kopiere diese Datei als .env und passe sie an.

# Web-Port auf dem Host (Standard: 8080)
TDR_PORT=8080

# Ollama-Modell das beim ersten Start automatisch gezogen wird
OLLAMA_MODEL=llama3

# Embedding-Modell (wird von HuggingFace geladen, dann lokal gecacht)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
